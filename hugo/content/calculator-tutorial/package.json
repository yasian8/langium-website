{
  "name": "calculator-tutorial",
  "version": "1.0.0",
  "main": "build/cli.js",
  "author": "Markus Rudolph",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "npm run build && rimraf test && mkdir test && node build/cli.js init -o test -z"
  },
  "dependencies": {
    "@types/rimraf": "^3.0.2",
    "@types/terminal-kit": "^1.34.1",
    "@types/yargs": "^17.0.10",
    "rimraf": "^3.0.2",
    "terminal-kit": "^2.4.0",
    "yargs": "^17.5.1",
    "zip-a-folder": "^1.1.5"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
